{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Default values for all YouVersion Suggest preferences",
  "type": "object",
  "properties": {
    "language": {
      "description": "The preferred language to use for all Bible references",
      "type": "string",
      "pattern": "^[a-z]{3}(_[a-z]{2,3})?$"
    },
    "version": {
      "description": "The preferred version (in the current preferred language) to use as the default for Bible references",
      "type": "integer",
      "minimum": 1
    },
    "refformat": {
      "description": "The format string for all copied Bible references",
      "type": "string"
    },
    "versenumbers": {
      "description": "Whether or not to include verse numbers in copied Bible references",
      "type": "boolean"
    }
  },
  "required": ["language", "version", "refformat", "versenumbers"],
  "additionalProperties": false
}
